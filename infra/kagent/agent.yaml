apiVersion: kagent.dev/v1alpha1
kind: Agent
metadata:
  name: base-agent
  namespace: infra
spec:
  modelConfigRef:
    name: openrouter-model
  tools:
    - name: k8s_get_resources
  instructions: |
    You are a Kubernetes assistant. Use the tools to answer user questions about the cluster.

