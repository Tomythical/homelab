apiVersion: apps/v1
kind: Deployment
metadata:
  name: whisper
  namespace: n8n
spec:
  replicas: 1
  selector:
    matchLabels:
      app: whisper
  template:
    metadata:
      labels:
        app: whisper
    spec:
      containers:
        - name: whisper
          image: ghcr.io/tomythical/whisper:latest
          ports:
            - containerPort: 5632
          resources:
            requests:
              cpu: "500m"
              memory: "500Mi"
            limits:
              cpu: "1"
              memory: "2Gi"
          readinessProbe:
            httpGet:
              path: /health
              port: 5632
            initialDelaySeconds: 10
            periodSeconds: 5
          livenessProbe:
            httpGet:
              path: /health
              port: 5632
            initialDelaySeconds: 30
            periodSeconds: 10
