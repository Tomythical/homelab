apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: hermes-webui
spec:
  ingressClassName: tailscale
  defaultBackend:
    service:
      name: hermes-agent
      port:
        number: 8787
  tls:
    - hosts:
        - hermes-webui
