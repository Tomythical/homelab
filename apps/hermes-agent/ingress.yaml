apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: hermes-agent
spec:
  ingressClassName: tailscale
  defaultBackend:
    service:
      name: hermes-agent
      port:
        number: 9119
  tls:
    - hosts:
        - hermes-agent