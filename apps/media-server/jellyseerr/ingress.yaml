---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: seerr
spec:
  ingressClassName: tailscale
  defaultBackend:
    service:
      name: seerr
      port:
        number: 80
  tls:
    - hosts:
        - seerr 
