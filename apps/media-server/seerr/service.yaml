apiVersion: v1
kind: Service
metadata:
  name: seerr
  namespace: media-server
spec:
  ports:
  - name: http
    port: 80
    protocol: TCP
    targetPort: http
  selector:
    app.kubernetes.io/instance: seerr
    app.kubernetes.io/name: seerr-chart
  type: ClusterIP
