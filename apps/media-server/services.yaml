apiVersion: v1
kind: Service
metadata:
  name: sonarr
  annotations:
    tailscale.com/expose: "true"
spec:
  selector:
    app: sonarr
  ports:
    - port: 8989
      name: sonarr-port
  type: LoadBalancer
  loadBalancerClass: tailscale

