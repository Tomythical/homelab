apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: streamystats-db-pvc
  namespace: media-server
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi
  storageClassName: local-path
