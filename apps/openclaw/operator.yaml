apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: openclaw-operator
spec:
  interval: 1h
  chartRef:
    kind: OCIRepository
    name: openclaw-operator-repo
    namespace: openclaw
  values:
    serviceAccount:
      annotations:
      tailscale.com/expose: "true"
